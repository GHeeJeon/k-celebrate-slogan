## 변경 내용

<!-- 라이브러리에서 무엇을 추가/수정했는지 설명해주세요 -->

## 체크리스트

- [ ] `package.json` 버전을 올렸다 (feat → minor, fix → patch)
- [ ] `npm run test` 통과
- [ ] `npm run build` 통과
- [ ] PR 머지 후 `git tag v<버전>` 및 `git push origin v<버전>` 으로 npm 배포 예정
