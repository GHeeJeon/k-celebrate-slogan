## Changes

<!-- Describe what you added/modified in the library -->

## Checklist

- [ ] Bumped `package.json` version (feat → minor, fix → patch)
- [ ] Passed `npm run test`
- [ ] Passed `npm run build`
- [ ] Plan to deploy to npm via `git tag v<version>` and `git push origin v<version>` after PR merge
